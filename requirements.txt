# =============================================================================
# Chronos-2 Forecasting Tool — Production Dependencies
#
# Pin all production dependencies exactly.  Update via smoke test.
# =============================================================================

streamlit==1.60.0
pandas>=2.2,<3.1
numpy>=1.26,<2.0
pyarrow>=15.0

# Chronos-2 inference (CPU-compatible)
# Install via: pip install "chronos-forecasting>=2.2"
chronos-forecasting>=2.2.0

# PyTorch (CPU-only is sufficient for development)
# Install via: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# ---------------------------------------------------------------------------
# Development dependencies
# ---------------------------------------------------------------------------
pytest>=8.0
pytest-cov>=5.0
psutil>=5.9
matplotlib>=3.7

# ---------------------------------------------------------------------------
# Notes
# ---------------------------------------------------------------------------
# - chronos-forecasting pulls in transformers and torch as dependencies.
# - For CPU-only environments, pre-install torch with the cpu-only index.
# - For Streamlit Community Cloud, the cloud runtime includes torch CPU.