# =============================================================================
# Chronos-2 Forecasting Tool — Production Dependencies
#
# Pin every direct runtime dependency exactly after a verified test.
# Last resolved: 2026-07-28 with Python 3.12
# =============================================================================

streamlit==1.60.0
pandas==3.0.5
numpy==2.4.6
pyarrow==24.0.0
chronos-forecasting==2.3.1
torch==2.13.0

# ---------------------------------------------------------------------------
# IMPORTANT: CPU-only PyTorch installation
#
# The pinned torch==2.13.0 above resolves to the CUDA-enabled package by
# default. To install the CPU-only variant, pip MUST use the CPU-only index:
#
#   PIP_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cpu
#
# Set this environment variable in:
#   - GitHub Actions CI (already configured)
#   - Streamlit Community Cloud secrets/settings
#   - Local D: drive environment (already configured in activate script)
#
# To verify CPU-only installation after pip install:
#   python -c "import torch; assert torch.version.cuda is None"
#
# This environment variable is the only supported mechanism. Do NOT add a
# separate `pip install torch --index-url ...` step -- that would diverge
# from the Cloud installation path.
# ---------------------------------------------------------------------------
