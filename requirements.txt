# =============================================================================
# Chronos-2 Forecasting Tool — Production Dependencies
#
# Pin every direct runtime dependency exactly after a verified test.
# Last resolved: 2026-07-28 with Python 3.12
# =============================================================================

--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.60.0
pandas==3.0.5
numpy==2.4.6
pyarrow==24.0.0
chronos-forecasting==2.3.1
torch==2.13.0

# ---------------------------------------------------------------------------
# CPU-only PyTorch installation
#
# The --extra-index-url directive above tells pip to use the PyTorch CPU
# wheel index as an additional source when resolving torch==2.13.0, so the
# CPU-only build is installed by default without requiring an externally
# injected PIP_EXTRA_INDEX_URL environment variable.
#
# To verify CPU-only installation after pip install:
#   python -c "import torch; assert torch.version.cuda is None"
#
# Streamlit Community Cloud reads requirements.txt directly and will
# use the extra index URL from this directive.
#
# NOTE on dependency-confusion risk: adding an extra index URL means pip
# searches both PyPI and the extra index. The pinned exact version ensures
# deterministic resolution. Review the index order when changing packages.
# ---------------------------------------------------------------------------
